# To install all dependencies at once, run: pip install -r requirements.txt

# --- Core Dependencies (AI & Database) ---
# Sentence-Transformers: Used for computing text embeddings.
sentence-transformers

# FAISS: Used for high-speed vector indexing and search.
faiss-cpu         # Use faiss-gpu if you have a compatible NVIDIA GPU

# NLTK: Used for keyword extraction (finding nouns, verbs, etc.).
nltk

# wordfreq: general-language word frequencies (FR/EN Zipf) powering the rarity filter.
wordfreq

# NumPy: Required for numerical operations.
numpy

# --- Web Server Dependencies ---
# FastAPI: The framework used to build your API server.
fastapi

# Uvicorn: The ASGI server used to run the FastAPI application.
uvicorn[standard]

# python-multipart: required by FastAPI to parse file/form uploads (corpus upload).
python-multipart

# --- Additional Utilities ---
pydantic
pypdf
spacy>=3.8
fr_core_news_md @ https://github.com/explosion/spacy-models/releases/download/fr_core_news_md-3.8.0/fr_core_news_md-3.8.0-py3-none-any.whl

# --- LLM Support ---
google-generativeai
requests
python-dotenv

# --- Durable storage for user-uploaded corpora (Cloudflare R2 via S3 API) ---
# Optional at runtime: without R2_* env vars the app persists uploads to a
# local user_data/ folder instead.
boto3
# transformers  # Uncomment if you want to use local HuggingFace models directly
# torch         # Required for transformers


